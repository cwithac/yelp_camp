<% include partials/head %>

<h1>Campgrounds</h1>

<a href="/campgrounds/create">Add New</a>

<ul>
  <% campgrounds.forEach(campground => { %>
      <div>
        <h4><%= campground.name %></h4>
        <img src="<%= campground.image %>" alt="">
      </div>
  <% }) %>
</ul>

<% include partials/foot %>
