<% include ../partials/head %>

<% include ../partials/nav %>

<% include ../partials/alerts %>

<main class="container">
  <h1 class="text-center">Login</h1>
  <div class="login-register">
    <form action="/users/login" method="POST">
      <div class="form-group">
        <input class="form-control" pattern="^[A-Za-z0-9]+$" title="May only contain a-z and 0-9" type="text" name="username" placeholder="username (case sensitive)" required>
        </div>
        <div class="form-group">
        <input class="form-control" pattern="^[A-Za-z0-9]+$" title="May only contain a-z and 0-9" type="password" name="password" placeholder="password (case sensitive)" required>
        </div>
        <div class="form-group">
        <button class="btn btn-lg btn-primary btn-block">Login</button>
        </div>
    </form>
      <a href="/campgrounds"><span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> Back to Campgrounds</a>
  </div>
</main>

<% include ../partials/foot %>
